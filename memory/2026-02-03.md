# 2026-02-03 Daily Notes

## 飞书配置任务 (待办)

**用户**: Does / suyugan1
**目标**: 配置飞书机器人对接OpenClaw

### 步骤
1. 用户需在飞书开放平台创建应用
2. 获取App ID和App Secret
3. 配置OpenClaw飞书插件

### OpenClaw配置命令
```
openclaw plugins install @m1heng-clawd/feishu
openclaw config set channels.feishu.appId "xxx"
openclaw config set channels.feishu.appSecret "xxx"
openclaw config set channels.feishu.enabled true
openclaw config set channels.feishu.connectionMode websocket
openclaw config set channels.feishu.dmPolicy pairing
openclaw config set channels.feishu.groupPolicy allowlist
openclaw config set channels.feishu.requireMention true
openclaw gateway restart
```

### 飞书端配置
- 事件和回调 → 长连接模式
- 添加"接收消息"事件
- 开通权限：contact:user.base:readonly, im:message

### 状态
等待用户提供App ID和App Secret