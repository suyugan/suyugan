# 2026-02-11 工作日志

## 微信插件 WCGroupSync - 调试过程

### 闪退排查（关键！）
- v2: LOAD_WEAK_DYLIB注入正确，但constructor没生效（`__mod_init_func`缺失）
- v3: 加了`-Wl,-init,_WCGroupSync_Init` + 非static + `__attribute__((used))`，constructor生效了
- v3: 点设置闪退 → v4去掉设置页hook
- v4: 还是闪退（打开微信就崩）→ v5延迟constructor 3秒
- v6: 延迟3秒还崩 → 测试空dylib(只有NSLog)不崩
- v7: 加回消息hook+HTTP推送(无昵称/头像) → 发消息就崩
- **v8: hook但回调完全空操作(只调原始方法) → 等待测试结果**
- 如果v8不崩 = 访问msgWrap属性方式有问题
- 如果v8崩 = AsyncOnAddMsg:MsgWrap: 方法签名不对，需要class-dump

### 编译流程
- GitHub Actions: `suyugan/wechat-group-sync`, macOS-14 runner
- workflow文件当前编译的是 WCGroupSync_minimal.m（测试版）
- 正式版源码在 WCGroupSync.m

### 已确认能工作的
- 数据已收到43条消息（群38669900382@chatroom）— 这是之前某个版本成功推送的
- Dashboard: http://bm.weiixxin.com/wechat-sync/ （已修复API路径和消息解析）
- Showcase: http://bm.weiixxin.com/wechat-sync/showcase （已仿造laolin.ai/showcase）

### 服务器端更新
- 数据库加了 sender_avatar 列
- server.js INSERT加了 sender_avatar 字段
- 前端showcase支持显示头像图片（有头像显示img，无头像显示首字母）

## Showcase页面
- 仿照 laolin.ai/showcase，7个模块
- 苏总说缺"群聊"模块 → 已加上最新群聊消息列表
- API路径修复：`const API = window.location.origin + "/wechat-sync"`
- 消息内容解析：parseContent函数处理XML/表情/图片等

## 中转API确认
- 已对接林杰中转：baseUrl = http://m.laolin.me:3002
- 配置完整，正在使用中

## 苏总新要求
- 账号密码双重存储：本地 + 服务器 `/home/ubuntu/.credentials/accounts.md`
- 需要账号密码先SSH服务器查，没有再问
- 重要信息立刻写文件，不能忘
- 苏总问为什么经常忘记 → 因为对话压缩+session重启+记笔记不勤
