# 2026-02-17 工作日志

## 讨好型人格视频制作
- 22张即梦配图全部生成（scene_01~22.webp）
- 第一版视频合成完成但字幕不同步（drawtext平铺，没用ASR时间戳）
- 第二版用FunASR Paraformer-zh逐字时间戳+原始文案对齐，字幕精确同步
- 最终交付: http://bm.weiixxin.com/videos/taohaoxing.mp4
- 自评6分：字幕同步了但场景数不够（22个偏少），动效不够

## 即梦并发生图方案（重要！）
- 写了 `D:\video-analysis\scripts\jimeng_batch_concurrent.py` 批量并发脚本
- 核心思路：一次性提交多个generate请求（间隔2秒），收集submit_id，批量poll轮询
- 10张图并发3分半完成 vs 串行10分钟都没完
- 已写入AGENTS.md和douyin-clone SKILL.md（方案A推荐，方案B串行备用）

## 标准化上传脚本
- 写了 `scripts\upload_to_cloud.ps1`：上传→chmod→验证链接，三步一体
- 已写入TOOLS.md，以后子代理统一用这个脚本上传

## Nginx一次性修复
- 子代理修复了腾讯云nginx配置，/var/www/html/下任何文件都能直接访问
- 根本原因分析：每次手动scp忘chmod，nginx没配通用规则

## douyin-clone技能更新（多项重要更新）
1. **并发生图方案**写入5.1节（方案A推荐）
2. **色彩层次要求**：提示词必须加肤色、暖光、阴影渐变，不能只写单色调
3. **场景数量要求**：最低25个场景，每5-8秒换画面，新增scene_table.md
4. **字体打包方案**：抖音体放项目目录，FFmpeg用fontsdir指定，不依赖系统安装
5. **风格样片**：批量生图前先出2-3张测试图跟参考帧对比
6. **动画模式**（5.1c）：火柴人/简笔画用Remotion生成，SVG+React控制动画
7. **声音复刻**（5.2重写）：GPT-SoVITS音色克隆、语速匹配、停顿节奏、音量匹配
8. **流水线自动推进规则**写入AGENTS.md

## AGENTS.md规则更新
- 流水线任务自动推进，不停下来问
- 有技能就用技能，不临时拼凑
- 任务完成附带能力总结
- 即梦生图最佳实践（并发模式）

## 当前进行中的任务
- 抖音复刻 https://v.douyin.com/dPnzZtdxre8/
  - 识别为火柴人动画模式，35个场景
  - TTS+BGM混音已完成（76秒）
  - 即梦生图35张进行中（子代理: agent:product:subagent:142c9b29-a3f6-4614-b06d-d8c7102eac46）
  - 工作目录: D:\video-analysis\output\brave-be-yourself\
  - 下一步：生图完成后→视频合成+字幕同步+上传

## 重要纠错
- **选题环节被跳过**：技能写了阶段2.5推送选题给用户选，但spawn时没写进task，子代理自己选了题
- **动画模式不该生图**：识别为火柴人动画模式后应走Remotion纯代码渲染（5.1c），不需要即梦生图。我错误地按配图口播思路安排了生图子代理
- 苏总明确指出：Remotion生成动画就不需要即梦生成图片，生成图片是为了合成视频（配图口播模式）
- 需要停掉即梦生图子代理，改用Remotion方案

## 教训
- 有成熟技能的任务必须调用技能，临时拼凑质量差（字幕不同步就是例子）
- 多步骤流水线不要停下来问，自动推进到下一步
- 反复出错的操作要封装成标准脚本（upload_to_cloud.ps1）
- 子代理spawn时写明每步推送进度通知
